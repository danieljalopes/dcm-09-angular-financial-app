export * from 'fullcalendar/public-api';
import * as i0 from '@angular/core';
import { Component, ViewEncapsulation, ChangeDetectionStrategy, Input, ViewChild, ContentChild, NgModule } from '@angular/core';
import { Calendar } from 'fullcalendar';
import { CustomRenderingStore } from 'fullcalendar/protected-api';
import * as i1 from '@angular/common';
import { CommonModule } from '@angular/common';

const OPTION_IS_DEEP = {
    headerToolbar: true,
    footerToolbar: true,
    events: true,
    eventSources: true,
    resources: true
};
/*
NOTE: keep synced with component
*/
const OPTION_INPUT_NAMES = [
    'events',
    'eventSources',
    'resources',
];

const hasOwnProperty = Object.prototype.hasOwnProperty;
/*
Really simple clone utility. Only copies plain arrays, objects, and Dates. Transfers everything else as-is.
Wanted to use a third-party lib, but none did exactly this.
*/
function deepCopy(input) {
    if (Array.isArray(input)) {
        return input.map(deepCopy);
    }
    else if (input instanceof Date) {
        return new Date(input.valueOf());
    }
    else if (typeof input === 'object' && input) { // non-null object
        return mapHash(input, deepCopy);
    }
    else { // everything else (null, function, etc)
        return input;
    }
}
function mapHash(input, func) {
    const output = {};
    for (const key in input) {
        if (hasOwnProperty.call(input, key)) {
            output[key] = func(input[key], key);
        }
    }
    return output;
}

/*
Forked from https://github.com/epoberezkin/fast-deep-equal (also has MIT license)
Needed ESM support or else Angular complains about treeshaking
(https://github.com/fullcalendar/fullcalendar-angular/issues/421)
*/
function deepEqual(a, b) {
    if (a === b)
        return true;
    if (a && b && typeof a == 'object' && typeof b == 'object') {
        if (a.constructor !== b.constructor)
            return false;
        var length, i, keys;
        if (Array.isArray(a)) {
            length = a.length;
            if (length != b.length)
                return false;
            for (i = length; i-- !== 0;)
                if (!deepEqual(a[i], b[i]))
                    return false;
            return true;
        }
        if (a.constructor === RegExp)
            return a.source === b.source && a.flags === b.flags;
        if (a.valueOf !== Object.prototype.valueOf)
            return a.valueOf() === b.valueOf();
        if (a.toString !== Object.prototype.toString)
            return a.toString() === b.toString();
        keys = Object.keys(a);
        length = keys.length;
        if (length !== Object.keys(b).length)
            return false;
        for (i = length; i-- !== 0;)
            if (!Object.prototype.hasOwnProperty.call(b, keys[i]))
                return false;
        for (i = length; i-- !== 0;) {
            var key = keys[i];
            if (!deepEqual(a[key], b[key]))
                return false;
        }
        return true;
    }
    // true if both NaN, false otherwise
    return a !== a && b !== b;
}

const dummyContainer = typeof document !== 'undefined' ? document.createDocumentFragment() : null;
class OffscreenFragmentComponent {
    constructor(element) {
        this.element = element;
    }
    ngAfterViewInit() {
        if (dummyContainer) {
            dummyContainer.appendChild(this.element.nativeElement);
        }
    }
    // invoked BEFORE component removed from DOM
    ngOnDestroy() {
        if (dummyContainer) {
            dummyContainer.removeChild(this.element.nativeElement);
        }
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "16.2.12", ngImport: i0, type: OffscreenFragmentComponent, deps: [{ token: i0.ElementRef }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "16.2.12", type: OffscreenFragmentComponent, selector: "offscreen-fragment", ngImport: i0, template: '<ng-content></ng-content>', isInline: true, changeDetection: i0.ChangeDetectionStrategy.Default, encapsulation: i0.ViewEncapsulation.None }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "16.2.12", ngImport: i0, type: OffscreenFragmentComponent, decorators: [{
            type: Component,
            args: [{
                    selector: 'offscreen-fragment',
                    template: '<ng-content></ng-content>',
                    encapsulation: ViewEncapsulation.None,
                    changeDetection: ChangeDetectionStrategy.Default
                }]
        }], ctorParameters: function () { return [{ type: i0.ElementRef }]; } });

class TransportContainerComponent {
    ngAfterViewInit() {
        this.attachWrapperEl();
    }
    ngOnChanges(changes) {
        if (changes['containerEl'] && this.wrapperElRef) {
            this.attachWrapperEl();
        }
    }
    // Keep Preact's container in place and move only Angular's boxless wrapper into it.
    attachWrapperEl() {
        const wrapperEl = this.wrapperElRef?.nativeElement;
        if (wrapperEl && wrapperEl.parentNode !== this.containerEl) {
            this.containerEl.appendChild(wrapperEl);
        }
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "16.2.12", ngImport: i0, type: TransportContainerComponent, deps: [], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "16.2.12", type: TransportContainerComponent, selector: "transport-container", inputs: { containerEl: "containerEl", template: "template", renderProps: "renderProps" }, viewQueries: [{ propertyName: "wrapperElRef", first: true, predicate: ["wrapperEl"], descendants: true }], usesOnChanges: true, ngImport: i0, template: "<div #wrapperEl style=\"display: contents\">\n  <ng-container\n    [ngTemplateOutlet]=\"template\"\n    [ngTemplateOutletContext]=\"{ $implicit: renderProps }\"\n  ></ng-container>\n</div>\n", dependencies: [{ kind: "directive", type: i1.NgTemplateOutlet, selector: "[ngTemplateOutlet]", inputs: ["ngTemplateOutletContext", "ngTemplateOutlet", "ngTemplateOutletInjector"] }], changeDetection: i0.ChangeDetectionStrategy.Default, encapsulation: i0.ViewEncapsulation.None }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "16.2.12", ngImport: i0, type: TransportContainerComponent, decorators: [{
            type: Component,
            args: [{ selector: 'transport-container', encapsulation: ViewEncapsulation.None, changeDetection: ChangeDetectionStrategy.Default, template: "<div #wrapperEl style=\"display: contents\">\n  <ng-container\n    [ngTemplateOutlet]=\"template\"\n    [ngTemplateOutletContext]=\"{ $implicit: renderProps }\"\n  ></ng-container>\n</div>\n" }]
        }], propDecorators: { containerEl: [{
                type: Input
            }], template: [{
                type: Input
            }], renderProps: [{
                type: Input
            }], wrapperElRef: [{
                type: ViewChild,
                args: ['wrapperEl']
            }] } });

class FullCalendarComponent {
    constructor(element, changeDetector) {
        this.element = element;
        this.calendar = null;
        this.optionSnapshot = {}; // for diffing
        this.customRenderingMap = new Map();
        const customRenderingStore = new CustomRenderingStore();
        customRenderingStore.subscribe((customRenderingMap) => {
            this.customRenderingMap = customRenderingMap;
            this.customRenderingArray = undefined; // clear cache
            changeDetector.detectChanges();
        });
        this.handleCustomRendering = customRenderingStore.handle.bind(customRenderingStore);
    }
    ngAfterViewInit() {
        const { deepChangeDetection } = this;
        const options = {
            ...this.options,
            ...this.buildInputOptions(),
        };
        // initialize snapshot
        this.optionSnapshot = mapHash(options, (optionVal, optionName) => ((deepChangeDetection && OPTION_IS_DEEP[optionName])
            ? deepCopy(optionVal)
            : optionVal));
        const calendarEl = this.element.nativeElement;
        const calendar = this.calendar = new Calendar(calendarEl, {
            ...options,
            ...this.buildExtraOptions(),
        });
        // Ionic dimensions hack
        // https://github.com/fullcalendar/fullcalendar/issues/4976
        const ionContent = calendarEl.closest('ion-content');
        if (ionContent && ionContent.componentOnReady) {
            ionContent.componentOnReady().then(() => {
                window.requestAnimationFrame(() => {
                    calendar.render();
                });
            });
        }
        else {
            calendar.render();
        }
    }
    /*
    allows us to manually detect complex input changes, internal mutations to certain options.
    called before ngOnChanges. called much more often than ngOnChanges.
    */
    ngDoCheck() {
        if (this.calendar) { // not the initial render
            const { deepChangeDetection, optionSnapshot } = this;
            const newOptions = {
                ...this.options,
                ...this.buildInputOptions(),
            };
            const newProcessedOptions = {};
            const changedOptionNames = [];
            // detect adds and updates (and update snapshot)
            for (const optionName in newOptions) {
                if (newOptions.hasOwnProperty(optionName)) {
                    let optionVal = newOptions[optionName];
                    if (deepChangeDetection && OPTION_IS_DEEP[optionName]) {
                        if (!deepEqual(optionSnapshot[optionName], optionVal)) {
                            optionSnapshot[optionName] = deepCopy(optionVal);
                            changedOptionNames.push(optionName);
                        }
                    }
                    else {
                        if (optionSnapshot[optionName] !== optionVal) {
                            optionSnapshot[optionName] = optionVal;
                            changedOptionNames.push(optionName);
                        }
                    }
                    newProcessedOptions[optionName] = optionVal;
                }
            }
            const oldOptionNames = Object.keys(optionSnapshot);
            // detect removals (and update snapshot)
            for (const optionName of oldOptionNames) {
                if (!(optionName in newOptions)) { // doesn't exist in new options?
                    delete optionSnapshot[optionName];
                    changedOptionNames.push(optionName);
                }
            }
            if (changedOptionNames.length) {
                this.calendar.pauseRendering();
                this.calendar.resetOptions({
                    ...newProcessedOptions,
                    ...this.buildExtraOptions(),
                }, changedOptionNames);
            }
        }
    }
    ngAfterContentChecked() {
        if (this.calendar) { // too defensive?
            this.calendar.resumeRendering();
        }
    }
    ngOnDestroy() {
        if (this.calendar) { // too defensive?
            this.calendar.destroy();
            this.calendar = null;
        }
    }
    get customRenderings() {
        return this.customRenderingArray ||
            (this.customRenderingArray = [...this.customRenderingMap.values()]);
    }
    getApi() {
        return this.calendar;
    }
    buildInputOptions() {
        const options = {};
        for (const inputName of OPTION_INPUT_NAMES) {
            const inputValue = this[inputName];
            if (inputValue != null) { // exclude both null and undefined
                options[inputName] = inputValue;
            }
        }
        return options;
    }
    buildExtraOptions() {
        return {
            handleCustomRendering: this.handleCustomRendering,
            customRenderingMetaMap: this,
        };
    }
    // for `trackBy` in loop
    trackCustomRendering(index, customRendering) {
        return customRendering.id;
    }
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "16.2.12", ngImport: i0, type: FullCalendarComponent, deps: [{ token: i0.ElementRef }, { token: i0.ChangeDetectorRef }], target: i0.ɵɵFactoryTarget.Component }); }
    static { this.ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "16.2.12", type: FullCalendarComponent, selector: "full-calendar", inputs: { options: "options", deepChangeDetection: "deepChangeDetection", events: "events", eventSources: "eventSources", resources: "resources" }, queries: [{ propertyName: "allDayHeaderContent", first: true, predicate: ["allDayHeaderContent"], descendants: true, static: true }, { propertyName: "dayCellTopContent", first: true, predicate: ["dayCellTopContent"], descendants: true, static: true }, { propertyName: "dayHeaderContent", first: true, predicate: ["dayHeaderContent"], descendants: true, static: true }, { propertyName: "eventContent", first: true, predicate: ["eventContent"], descendants: true, static: true }, { propertyName: "inlineWeekNumberContent", first: true, predicate: ["inlineWeekNumberContent"], descendants: true, static: true }, { propertyName: "listDayHeaderContent", first: true, predicate: ["listDayHeaderContent"], descendants: true, static: true }, { propertyName: "moreLinkContent", first: true, predicate: ["moreLinkContent"], descendants: true, static: true }, { propertyName: "noEventsContent", first: true, predicate: ["noEventsContent"], descendants: true, static: true }, { propertyName: "nowIndicatorHeaderContent", first: true, predicate: ["nowIndicatorHeaderContent"], descendants: true, static: true }, { propertyName: "nowIndicatorLineContent", first: true, predicate: ["nowIndicatorLineContent"], descendants: true, static: true }, { propertyName: "popoverCloseContent", first: true, predicate: ["popoverCloseContent"], descendants: true, static: true }, { propertyName: "resourceCellContent", first: true, predicate: ["resourceCellContent"], descendants: true, static: true }, { propertyName: "resourceColumnHeaderContent", first: true, predicate: ["resourceColumnHeaderContent"], descendants: true, static: true }, { propertyName: "resourceDayHeaderContent", first: true, predicate: ["resourceDayHeaderContent"], descendants: true, static: true }, { propertyName: "resourceExpanderContent", first: true, predicate: ["resourceExpanderContent"], descendants: true, static: true }, { propertyName: "resourceGroupHeaderContent", first: true, predicate: ["resourceGroupHeaderContent"], descendants: true, static: true }, { propertyName: "resourceGroupLaneContent", first: true, predicate: ["resourceGroupLaneContent"], descendants: true, static: true }, { propertyName: "resourceLaneBottomContent", first: true, predicate: ["resourceLaneBottomContent"], descendants: true, static: true }, { propertyName: "resourceLaneTopContent", first: true, predicate: ["resourceLaneTopContent"], descendants: true, static: true }, { propertyName: "rowEventAfterContent", first: true, predicate: ["rowEventAfterContent"], descendants: true, static: true }, { propertyName: "rowEventBeforeContent", first: true, predicate: ["rowEventBeforeContent"], descendants: true, static: true }, { propertyName: "slotHeaderContent", first: true, predicate: ["slotHeaderContent"], descendants: true, static: true }, { propertyName: "weekNumberHeaderContent", first: true, predicate: ["weekNumberHeaderContent"], descendants: true, static: true }], ngImport: i0, template: `
    <offscreen-fragment>
      <transport-container *ngFor="let customRendering of customRenderings; trackBy:trackCustomRendering"
        [containerEl]="customRendering.containerEl"
        [template]="customRendering.generatorMeta"
        [renderProps]="customRendering.renderProps"
      ></transport-container>
    </offscreen-fragment>
  `, isInline: true, dependencies: [{ kind: "directive", type: i1.NgForOf, selector: "[ngFor][ngForOf]", inputs: ["ngForOf", "ngForTrackBy", "ngForTemplate"] }, { kind: "component", type: OffscreenFragmentComponent, selector: "offscreen-fragment" }, { kind: "component", type: TransportContainerComponent, selector: "transport-container", inputs: ["containerEl", "template", "renderProps"] }], changeDetection: i0.ChangeDetectionStrategy.Default, encapsulation: i0.ViewEncapsulation.None }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "16.2.12", ngImport: i0, type: FullCalendarComponent, decorators: [{
            type: Component,
            args: [{
                    selector: 'full-calendar',
                    template: `
    <offscreen-fragment>
      <transport-container *ngFor="let customRendering of customRenderings; trackBy:trackCustomRendering"
        [containerEl]="customRendering.containerEl"
        [template]="customRendering.generatorMeta"
        [renderProps]="customRendering.renderProps"
      ></transport-container>
    </offscreen-fragment>
  `,
                    encapsulation: ViewEncapsulation.None,
                    changeDetection: ChangeDetectionStrategy.Default
                }]
        }], ctorParameters: function () { return [{ type: i0.ElementRef }, { type: i0.ChangeDetectorRef }]; }, propDecorators: { options: [{
                type: Input
            }], deepChangeDetection: [{
                type: Input
            }], events: [{
                type: Input
            }], eventSources: [{
                type: Input
            }], resources: [{
                type: Input
            }], allDayHeaderContent: [{
                type: ContentChild,
                args: ['allDayHeaderContent', { static: true }]
            }], dayCellTopContent: [{
                type: ContentChild,
                args: ['dayCellTopContent', { static: true }]
            }], dayHeaderContent: [{
                type: ContentChild,
                args: ['dayHeaderContent', { static: true }]
            }], eventContent: [{
                type: ContentChild,
                args: ['eventContent', { static: true }]
            }], inlineWeekNumberContent: [{
                type: ContentChild,
                args: ['inlineWeekNumberContent', { static: true }]
            }], listDayHeaderContent: [{
                type: ContentChild,
                args: ['listDayHeaderContent', { static: true }]
            }], moreLinkContent: [{
                type: ContentChild,
                args: ['moreLinkContent', { static: true }]
            }], noEventsContent: [{
                type: ContentChild,
                args: ['noEventsContent', { static: true }]
            }], nowIndicatorHeaderContent: [{
                type: ContentChild,
                args: ['nowIndicatorHeaderContent', { static: true }]
            }], nowIndicatorLineContent: [{
                type: ContentChild,
                args: ['nowIndicatorLineContent', { static: true }]
            }], popoverCloseContent: [{
                type: ContentChild,
                args: ['popoverCloseContent', { static: true }]
            }], resourceCellContent: [{
                type: ContentChild,
                args: ['resourceCellContent', { static: true }]
            }], resourceColumnHeaderContent: [{
                type: ContentChild,
                args: ['resourceColumnHeaderContent', { static: true }]
            }], resourceDayHeaderContent: [{
                type: ContentChild,
                args: ['resourceDayHeaderContent', { static: true }]
            }], resourceExpanderContent: [{
                type: ContentChild,
                args: ['resourceExpanderContent', { static: true }]
            }], resourceGroupHeaderContent: [{
                type: ContentChild,
                args: ['resourceGroupHeaderContent', { static: true }]
            }], resourceGroupLaneContent: [{
                type: ContentChild,
                args: ['resourceGroupLaneContent', { static: true }]
            }], resourceLaneBottomContent: [{
                type: ContentChild,
                args: ['resourceLaneBottomContent', { static: true }]
            }], resourceLaneTopContent: [{
                type: ContentChild,
                args: ['resourceLaneTopContent', { static: true }]
            }], rowEventAfterContent: [{
                type: ContentChild,
                args: ['rowEventAfterContent', { static: true }]
            }], rowEventBeforeContent: [{
                type: ContentChild,
                args: ['rowEventBeforeContent', { static: true }]
            }], slotHeaderContent: [{
                type: ContentChild,
                args: ['slotHeaderContent', { static: true }]
            }], weekNumberHeaderContent: [{
                type: ContentChild,
                args: ['weekNumberHeaderContent', { static: true }]
            }] } });

class FullCalendarModule {
    static { this.ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "16.2.12", ngImport: i0, type: FullCalendarModule, deps: [], target: i0.ɵɵFactoryTarget.NgModule }); }
    static { this.ɵmod = i0.ɵɵngDeclareNgModule({ minVersion: "14.0.0", version: "16.2.12", ngImport: i0, type: FullCalendarModule, declarations: [FullCalendarComponent,
            OffscreenFragmentComponent,
            TransportContainerComponent], imports: [CommonModule], exports: [FullCalendarComponent] }); }
    static { this.ɵinj = i0.ɵɵngDeclareInjector({ minVersion: "12.0.0", version: "16.2.12", ngImport: i0, type: FullCalendarModule, imports: [CommonModule] }); }
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "16.2.12", ngImport: i0, type: FullCalendarModule, decorators: [{
            type: NgModule,
            args: [{
                    declarations: [
                        FullCalendarComponent,
                        OffscreenFragmentComponent,
                        TransportContainerComponent
                    ],
                    imports: [
                        CommonModule
                    ],
                    exports: [
                        FullCalendarComponent
                    ]
                }]
        }] });

/*
 * Public API Surface of lib
 */

/**
 * Generated bundle index. Do not edit.
 */

export { FullCalendarComponent, FullCalendarModule };
//# sourceMappingURL=fullcalendar-angular.mjs.map
